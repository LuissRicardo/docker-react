language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t luissricardo/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true luissricardo/docker-react npm run test
